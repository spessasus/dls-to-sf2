{
  "devDependencies": {
    "esbuild": "^0.25.0",
    "spessasynth_lib": "latest"
  },
  "scripts": {
    "update": "npm update",
    "build": "esbuild index.js --format=esm --bundle --minify --outfile=index.min.js"
  }
}
