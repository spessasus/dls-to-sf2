{
  "devDependencies": {
    "esbuild": "^0.25.0",
    "spessasynth_lib": "^3.21.14"
  },
  "scripts": {
    "update": "npm update",
    "build": "esbuild index.js --format=esm --bundle --minify --outfile=index.min.js"
  }
}
